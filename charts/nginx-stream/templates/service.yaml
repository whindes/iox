apiVersion: v1
kind: Service
metadata:
  name: {{ include "mgx-basic.fullname" . }}
  labels:
    {{- include "mgx-basic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  clusterIP: {{ .Values.service.clusterIP }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mgx-basic.selectorLabels" . | nindent 4 }}
