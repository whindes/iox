apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mgx-basic.fullname" . }}-test-connection"
  labels:
    {{- include "mgx-basic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mgx-basic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
